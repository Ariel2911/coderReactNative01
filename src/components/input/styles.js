import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({  
  input: {
    borderStyle: 'solid',
    borderBottomColor:'violet',
    borderBottomWidth: 1,
    flex: 0.85,
  },
});
